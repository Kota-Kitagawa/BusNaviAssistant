FROM node:20-alpine

WORKDIR /app

# パッケージファイルをコピー
COPY package.json package-lock.json ./

# 依存関係をインストール
RUN npm ci

# ソースコードをコピー
COPY . .

# 本番ビルド
RUN npm run build

# ポートを公開（必要に応じて変更）
EXPOSE 3000

# 開発サーバーを起動
CMD ["npm", "run", "dev"]